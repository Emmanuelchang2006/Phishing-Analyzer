# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # Required for form/file upload support

# ── Configuration & validation ───────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1         # Loads config from .env via BaseSettings
python-dotenv==1.0.1

# ── HTTP client (async) ──────────────────────────────────────────────────────
httpx==0.27.0                    # Async HTTP for VirusTotal, AbuseIPDB (Phase 2+)

# ── Database (prepared for Phase 3) ─────────────────────────────────────────
sqlalchemy==2.0.30               # ORM — async engine will be wired in Phase 3
asyncpg==0.29.0                  # Async PostgreSQL driver
alembic==1.13.1                  # DB migrations

# ── Caching (prepared for Phase 3) ──────────────────────────────────────────
redis==5.0.4                     # Redis client — connection pool added in Phase 3
hiredis==2.3.2                   # C-extension parser, ~10× faster Redis parsing

# ── Logging & observability ──────────────────────────────────────────────────
structlog==24.1.0                # Structured JSON logging for production
rich==13.7.1                     # Pretty dev-mode console output

# ── Security utilities ───────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # JWT handling (auth in later phase)
passlib[bcrypt]==1.7.4            # Password hashing (auth in later phase)

# ── Email / domain analysis (Phase 2+) ──────────────────────────────────────
dnspython==2.6.1                 # DNS lookups for SPF/DKIM/DMARC checks
python-whois==0.9.4              # WHOIS domain registration lookups

# ── AI Verdict (Phase 4+) ────────────────────────────────────────────────────
google-genai>=1.0.0              # Google Gemini SDK — free tier at aistudio.google.com
